PORT=5000
# Replace this placeholder with a unique random value of at least 32 characters.
JWT_SECRET=change_this_to_a_secure_random_string
JWT_EXPIRE=7d
PORTAL_JWT_EXPIRE=24h
# Comma-separated production origins, for example: https://hms.example.com
CORS_ORIGINS=
# Optional. Relative paths are resolved from the server working directory.
# DB_PATH=hospital.db
# Demo portal payments are disabled in production unless explicitly enabled.
ALLOW_SIMULATED_PAYMENTS=false
